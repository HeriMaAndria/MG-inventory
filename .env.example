# ============================================
# CONFIGURATION SUPABASE - MG INVENTORY
# ============================================

# 📝 INSTRUCTIONS :
# 1. Copie ce fichier en .env.local pour le développement local :
#    cp .env.example .env.local
#
# 2. Va sur https://supabase.com/dashboard et crée un projet
#
# 3. Dans Settings > API, copie :
#    - Project URL → NEXT_PUBLIC_SUPABASE_URL
#    - anon public key → NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# 4. Pour Vercel (production) :
#    - Va dans ton projet Vercel > Settings > Environment Variables
#    - Ajoute ces mêmes variables
#    - Redéploie

# ⚠️ SÉCURITÉ :
# - NE JAMAIS commit .env.local dans Git (déjà dans .gitignore)
# - La clé "anon" est publique et sécurisée par les Row Level Security (RLS)
# - Ne mets JAMAIS la clé "service_role" ici (elle bypass RLS)

# ============================================
# VARIABLES REQUISES
# ============================================

# URL de ton projet Supabase
# Format: https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://ton-projet.supabase.co

# Clé publique anonyme (sécurisée par RLS)
# Format: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=ta-cle-anon-ici

# ============================================
# VÉRIFICATION
# ============================================

# Pour tester que tout fonctionne en local :
# 1. npm install
# 2. npm run dev
# 3. Ouvre http://localhost:3000/login
# 4. Si tu vois la console afficher une erreur Supabase, vérifie tes variables

# Pour tester en production (Vercel) :
# 1. Les variables doivent être dans Vercel Dashboard
# 2. Après ajout/modification, redéploie le projet
# 3. Vérifie les logs de build dans Vercel

# ============================================
# AIDE
# ============================================

# Problème de build ?
# → Vérifie que les variables sont bien dans Vercel
# → Les messages d'erreur apparaissent dans Vercel Build Logs

# Problème d'authentification ?
# → Vérifie que ton projet Supabase a les tables profiles configurées
# → Vérifie les Row Level Security (RLS) policies
# → Consulte /SUPABASE_SETUP.md dans le projet
